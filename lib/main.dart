import 'package:flutter/material.dart';

main() => runApp(PerguntaApp());

class PerguntaApp extends StatelessWidget {
  Widget build(BuildContext contex) {
    return MaterialApp(
      home: Text('ERROR CODE FAIL2!'),
    );
  }
}
